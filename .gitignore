# Local .terraform directories
      **/.terraform/*
      
      .terraform/providers/registry.terraform.io/hashicorp/aws/5.69.0/windows_386/terraform-provider-aws_v5.69.0_x5.exe

      # .tfstate files
      *.tfstate
      *.tfstate.*

      # Crash log files
      crash.log
      crash.*.log

      # Exclude all .tfvars files, which are likely to contain sensitive data
      *.tfvars
      *.tfvars.json

      # Ignore override files as they are usually used for local development
      override.tf
      override.tf.json
      *_override.tf
      *_override.tf.json

      # Ignore CLI configuration files
      .terraformrc
      terraform.rc